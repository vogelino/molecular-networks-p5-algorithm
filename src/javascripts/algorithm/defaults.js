const Immutable = require('immutable');

export default Immutable.Map({
	'groupsAmount': 1,
	'circleMinSize': 10,
	'lineWeight': 1,
	'backgroundColor': 'whitesmoke',
	'wrapperSize': 30,
	'mainBrightness': 50,
	'mainColor': '#000',
	'formMargin': 6
});
